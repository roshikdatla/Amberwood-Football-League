[phases.setup]
nixPkgs = ["python39", "pip"]

[phases.install]
cmds = ["cd fantasy-football-dashboard/MCP && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'No build needed for Python app'"]

[start]
cmd = "cd fantasy-football-dashboard/MCP && python api_server.py"