[build]
builder = "NIXPACKS"
buildCommand = "cd MCP && pip install -r requirements.txt"

[deploy]
startCommand = "cd MCP && python api_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production.variables]