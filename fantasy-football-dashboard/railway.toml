[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd fantasy-football-dashboard/MCP && python api_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production.variables]